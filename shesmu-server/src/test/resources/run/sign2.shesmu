Version 1;
Input test;

Define foo(string s)
 Where s == project && sha1_signature != "";

Olive
 foo("the_foo_study")
 Run ok With ok = signature_count == 2 && "project" In signature_names && library_size > 0 && "library_size" In signature_names;
